// node_modules/tsup/assets/esm_shims.js
import path from "path";
import { fileURLToPath } from "url";
var getFilename = () => fileURLToPath(import.meta.url);
var getDirname = () => path.dirname(getFilename());
var __dirname = /* @__PURE__ */ getDirname();

// lib/pkg/growtopia_wasm.js
var Event = class _Event {
  static __wrap(ptr) {
    const obj = Object.create(_Event.prototype);
    obj.__wbg_ptr = ptr;
    EventFinalization.register(obj, obj.__wbg_ptr, obj);
    return obj;
  }
  __destroy_into_raw() {
    const ptr = this.__wbg_ptr;
    this.__wbg_ptr = 0;
    EventFinalization.unregister(this);
    return ptr;
  }
  free() {
    const ptr = this.__destroy_into_raw();
    wasm.__wbg_event_free(ptr, 0);
  }
  /**
   * @returns {Packet | undefined}
   */
  packet() {
    const ret = wasm.event_packet(this.__wbg_ptr);
    return ret === 0 ? void 0 : Packet.__wrap(ret);
  }
  /**
   * @returns {number}
   */
  get channelId() {
    const ret = wasm.__wbg_get_event_channelId(this.__wbg_ptr);
    return ret;
  }
  /**
   * @returns {number}
   */
  get data() {
    const ret = wasm.__wbg_get_event_data(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @returns {number}
   */
  get eventType() {
    const ret = wasm.__wbg_get_event_eventType(this.__wbg_ptr);
    return ret;
  }
  /**
   * @returns {number}
   */
  get peerId() {
    const ret = wasm.__wbg_get_event_peerId(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @param {number} arg0
   */
  set channelId(arg0) {
    wasm.__wbg_set_event_channelId(this.__wbg_ptr, arg0);
  }
  /**
   * @param {number} arg0
   */
  set data(arg0) {
    wasm.__wbg_set_event_data(this.__wbg_ptr, arg0);
  }
  /**
   * @param {number} arg0
   */
  set eventType(arg0) {
    wasm.__wbg_set_event_eventType(this.__wbg_ptr, arg0);
  }
  /**
   * @param {number} arg0
   */
  set peerId(arg0) {
    wasm.__wbg_set_event_peerId(this.__wbg_ptr, arg0);
  }
};
if (Symbol.dispose) Event.prototype[Symbol.dispose] = Event.prototype.free;
var Host = class {
  __destroy_into_raw() {
    const ptr = this.__wbg_ptr;
    this.__wbg_ptr = 0;
    HostFinalization.unregister(this);
    return ptr;
  }
  free() {
    const ptr = this.__destroy_into_raw();
    wasm.__wbg_host_free(ptr, 0);
  }
  /**
   * @returns {number}
   */
  get id() {
    const ret = wasm.__wbg_get_host_id(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @param {number} channelId
   * @param {Packet} packet
   */
  broadcast(channelId, packet) {
    _assertClass(packet, Packet);
    wasm.host_broadcast(this.__wbg_ptr, channelId, packet.__wbg_ptr);
  }
  /**
   * @returns {number}
   */
  channelLimit() {
    const ret = wasm.host_channelLimit(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @returns {Event}
   */
  checkEvents() {
    const ret = wasm.host_checkEvents(this.__wbg_ptr);
    if (ret[2]) {
      throw takeFromExternrefTable0(ret[1]);
    }
    return Event.__wrap(ret[0]);
  }
  /**
   * @param {string} ip
   * @param {number} port
   * @param {number} channelCount
   * @param {number} data
   * @returns {number}
   */
  connect(ip, port, channelCount, data) {
    const ptr0 = passStringToWasm0(ip, wasm.__wbindgen_malloc, wasm.__wbindgen_realloc);
    const len0 = WASM_VECTOR_LEN;
    const ret = wasm.host_connect(this.__wbg_ptr, ptr0, len0, port, channelCount, data);
    if (ret[2]) {
      throw takeFromExternrefTable0(ret[1]);
    }
    return ret[0] >>> 0;
  }
  flush() {
    wasm.host_flush(this.__wbg_ptr);
  }
  /**
   * @returns {number}
   */
  mtuLimit() {
    const ret = wasm.host_mtuLimit(this.__wbg_ptr);
    return ret;
  }
  /**
   * @param {string} bindIp
   * @param {number} bindPort
   * @param {JsHostSettings} jsSettings
   * @param {Function} sendCallback
   */
  constructor(bindIp, bindPort, jsSettings, sendCallback) {
    const ptr0 = passStringToWasm0(bindIp, wasm.__wbindgen_malloc, wasm.__wbindgen_realloc);
    const len0 = WASM_VECTOR_LEN;
    _assertClass(jsSettings, JsHostSettings);
    const ret = wasm.host_new(ptr0, len0, bindPort, jsSettings.__wbg_ptr, sendCallback);
    if (ret[2]) {
      throw takeFromExternrefTable0(ret[1]);
    }
    this.__wbg_ptr = ret[0];
    HostFinalization.register(this, this.__wbg_ptr, this);
    return this;
  }
  /**
   * @param {number} peerId
   * @returns {string}
   */
  peerAddressIp(peerId) {
    let deferred1_0;
    let deferred1_1;
    try {
      const ret = wasm.host_peerAddressIp(this.__wbg_ptr, peerId);
      deferred1_0 = ret[0];
      deferred1_1 = ret[1];
      return getStringFromWasm0(ret[0], ret[1]);
    } finally {
      wasm.__wbindgen_free(deferred1_0, deferred1_1, 1);
    }
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerAddressPort(peerId) {
    const ret = wasm.host_peerAddressPort(this.__wbg_ptr, peerId);
    return ret;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerChannelCount(peerId) {
    const ret = wasm.host_peerChannelCount(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {boolean}
   */
  peerConnected(peerId) {
    const ret = wasm.host_peerConnected(this.__wbg_ptr, peerId);
    return ret !== 0;
  }
  /**
   * @param {number} peerId
   * @param {number} data
   */
  peerDisconnect(peerId, data) {
    wasm.host_peerDisconnect(this.__wbg_ptr, peerId, data);
  }
  /**
   * @param {number} peerId
   * @param {number} data
   */
  peerDisconnectLater(peerId, data) {
    wasm.host_peerDisconnectLater(this.__wbg_ptr, peerId, data);
  }
  /**
   * @param {number} peerId
   * @param {number} data
   */
  peerDisconnectNow(peerId, data) {
    wasm.host_peerDisconnectNow(this.__wbg_ptr, peerId, data);
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerIncomingBandwidth(peerId) {
    const ret = wasm.host_peerIncomingBandwidth(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerIncomingDataTotal(peerId) {
    const ret = wasm.host_peerIncomingDataTotal(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @returns {number}
   */
  peerLimit() {
    const ret = wasm.host_peerLimit(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerMtu(peerId) {
    const ret = wasm.host_peerMtu(this.__wbg_ptr, peerId);
    return ret;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerOutgoingBandwidth(peerId) {
    const ret = wasm.host_peerOutgoingBandwidth(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerOutgoingDataTotal(peerId) {
    const ret = wasm.host_peerOutgoingDataTotal(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerPacketLoss(peerId) {
    const ret = wasm.host_peerPacketLoss(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerPacketLossVariance(peerId) {
    const ret = wasm.host_peerPacketLossVariance(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerPacketsLost(peerId) {
    const ret = wasm.host_peerPacketsLost(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerPacketsSent(peerId) {
    const ret = wasm.host_peerPacketsSent(this.__wbg_ptr, peerId);
    return ret >>> 0;
  }
  /**
   * @param {number} peerId
   */
  peerPing(peerId) {
    wasm.host_peerPing(this.__wbg_ptr, peerId);
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerPingIntervalMs(peerId) {
    const ret = wasm.host_peerPingIntervalMs(this.__wbg_ptr, peerId);
    return ret;
  }
  /**
   * @param {number} peerId
   */
  peerReset(peerId) {
    wasm.host_peerReset(this.__wbg_ptr, peerId);
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerRoundTripTimeMs(peerId) {
    const ret = wasm.host_peerRoundTripTimeMs(this.__wbg_ptr, peerId);
    return ret;
  }
  /**
   * @param {number} peerId
   * @returns {number}
   */
  peerRoundTripTimeVarianceMs(peerId) {
    const ret = wasm.host_peerRoundTripTimeVarianceMs(this.__wbg_ptr, peerId);
    return ret;
  }
  /**
   * @param {number} peerId
   * @param {number} channelId
   * @param {Packet} packet
   */
  peerSend(peerId, channelId, packet) {
    _assertClass(packet, Packet);
    const ret = wasm.host_peerSend(this.__wbg_ptr, peerId, channelId, packet.__wbg_ptr);
    if (ret[1]) {
      throw takeFromExternrefTable0(ret[0]);
    }
  }
  /**
   * @param {number} peerId
   * @param {number} mtu
   */
  peerSetMtu(peerId, mtu) {
    const ret = wasm.host_peerSetMtu(this.__wbg_ptr, peerId, mtu);
    if (ret[1]) {
      throw takeFromExternrefTable0(ret[0]);
    }
  }
  /**
   * @param {number} peerId
   * @param {number} pingInterval
   */
  peerSetPingInterval(peerId, pingInterval) {
    wasm.host_peerSetPingInterval(this.__wbg_ptr, peerId, pingInterval);
  }
  /**
   * @param {number} peerId
   * @param {number} interval
   * @param {number} acceleration
   * @param {number} deceleration
   */
  peerSetThrottle(peerId, interval, acceleration, deceleration) {
    wasm.host_peerSetThrottle(this.__wbg_ptr, peerId, interval, acceleration, deceleration);
  }
  /**
   * @param {number} peerId
   * @param {number} limit
   * @param {number} minimum
   * @param {number} maximum
   */
  peerSetTimeout(peerId, limit, minimum, maximum) {
    wasm.host_peerSetTimeout(this.__wbg_ptr, peerId, limit, minimum, maximum);
  }
  /**
   * @param {number} peerId
   * @returns {PeerState}
   */
  peerState(peerId) {
    const ret = wasm.host_peerState(this.__wbg_ptr, peerId);
    return ret;
  }
  /**
   * @returns {Event}
   */
  service() {
    const ret = wasm.host_service(this.__wbg_ptr);
    if (ret[2]) {
      throw takeFromExternrefTable0(ret[1]);
    }
    return Event.__wrap(ret[0]);
  }
  /**
   * @param {number} incoming
   * @param {number} outgoing
   */
  setBandwidthLimit(incoming, outgoing) {
    const ret = wasm.host_setBandwidthLimit(this.__wbg_ptr, incoming, outgoing);
    if (ret[1]) {
      throw takeFromExternrefTable0(ret[0]);
    }
  }
  /**
   * @param {number} channelLimit
   */
  setChannelLimit(channelLimit) {
    const ret = wasm.host_setChannelLimit(this.__wbg_ptr, channelLimit);
    if (ret[1]) {
      throw takeFromExternrefTable0(ret[0]);
    }
  }
  /**
   * @param {number} mtu
   */
  setMtuLimit(mtu) {
    const ret = wasm.host_setMtuLimit(this.__wbg_ptr, mtu);
    if (ret[1]) {
      throw takeFromExternrefTable0(ret[0]);
    }
  }
  /**
   * @param {number} arg0
   */
  set id(arg0) {
    wasm.__wbg_set_host_id(this.__wbg_ptr, arg0);
  }
};
if (Symbol.dispose) Host.prototype[Symbol.dispose] = Host.prototype.free;
var JsHostSettings = class {
  __destroy_into_raw() {
    const ptr = this.__wbg_ptr;
    this.__wbg_ptr = 0;
    JsHostSettingsFinalization.unregister(this);
    return ptr;
  }
  free() {
    const ptr = this.__destroy_into_raw();
    wasm.__wbg_jshostsettings_free(ptr, 0);
  }
  /**
   * @returns {number}
   */
  get channelLimit() {
    const ret = wasm.__wbg_get_jshostsettings_channelLimit(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @returns {number}
   */
  get incomingBandwidth() {
    const ret = wasm.__wbg_get_jshostsettings_incomingBandwidth(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @returns {number}
   */
  get outgoingBandwidth() {
    const ret = wasm.__wbg_get_jshostsettings_outgoingBandwidth(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @returns {number}
   */
  get peerLimit() {
    const ret = wasm.__wbg_get_jshostsettings_peerLimit(this.__wbg_ptr);
    return ret >>> 0;
  }
  /**
   * @returns {boolean}
   */
  get useCrc32() {
    const ret = wasm.__wbg_get_jshostsettings_useCrc32(this.__wbg_ptr);
    return ret !== 0;
  }
  /**
   * @returns {boolean}
   */
  get useRangeCoder() {
    const ret = wasm.__wbg_get_jshostsettings_useRangeCoder(this.__wbg_ptr);
    return ret !== 0;
  }
  /**
   * @returns {boolean}
   */
  get usingNewPacketServer() {
    const ret = wasm.__wbg_get_jshostsettings_usingNewPacketServer(this.__wbg_ptr);
    return ret !== 0;
  }
  /**
   * @returns {boolean}
   */
  get usingNewPacket() {
    const ret = wasm.__wbg_get_jshostsettings_usingNewPacket(this.__wbg_ptr);
    return ret !== 0;
  }
  constructor() {
    const ret = wasm.jshostsettings_new();
    this.__wbg_ptr = ret;
    JsHostSettingsFinalization.register(this, this.__wbg_ptr, this);
    return this;
  }
  /**
   * @param {number} arg0
   */
  set channelLimit(arg0) {
    wasm.__wbg_set_jshostsettings_channelLimit(this.__wbg_ptr, arg0);
  }
  /**
   * @param {number} arg0
   */
  set incomingBandwidth(arg0) {
    wasm.__wbg_set_jshostsettings_incomingBandwidth(this.__wbg_ptr, arg0);
  }
  /**
   * @param {number} arg0
   */
  set outgoingBandwidth(arg0) {
    wasm.__wbg_set_jshostsettings_outgoingBandwidth(this.__wbg_ptr, arg0);
  }
  /**
   * @param {number} arg0
   */
  set peerLimit(arg0) {
    wasm.__wbg_set_jshostsettings_peerLimit(this.__wbg_ptr, arg0);
  }
  /**
   * @param {boolean} arg0
   */
  set useCrc32(arg0) {
    wasm.__wbg_set_jshostsettings_useCrc32(this.__wbg_ptr, arg0);
  }
  /**
   * @param {boolean} arg0
   */
  set useRangeCoder(arg0) {
    wasm.__wbg_set_jshostsettings_useRangeCoder(this.__wbg_ptr, arg0);
  }
  /**
   * @param {boolean} arg0
   */
  set usingNewPacketServer(arg0) {
    wasm.__wbg_set_jshostsettings_usingNewPacketServer(this.__wbg_ptr, arg0);
  }
  /**
   * @param {boolean} arg0
   */
  set usingNewPacket(arg0) {
    wasm.__wbg_set_jshostsettings_usingNewPacket(this.__wbg_ptr, arg0);
  }
};
if (Symbol.dispose) JsHostSettings.prototype[Symbol.dispose] = JsHostSettings.prototype.free;
var Packet = class _Packet {
  static __wrap(ptr) {
    const obj = Object.create(_Packet.prototype);
    obj.__wbg_ptr = ptr;
    PacketFinalization.register(obj, obj.__wbg_ptr, obj);
    return obj;
  }
  __destroy_into_raw() {
    const ptr = this.__wbg_ptr;
    this.__wbg_ptr = 0;
    PacketFinalization.unregister(this);
    return ptr;
  }
  free() {
    const ptr = this.__destroy_into_raw();
    wasm.__wbg_packet_free(ptr, 0);
  }
  /**
   * @returns {Uint8Array}
   */
  data() {
    const ret = wasm.packet_data(this.__wbg_ptr);
    var v1 = getArrayU8FromWasm0(ret[0], ret[1]).slice();
    wasm.__wbindgen_free(ret[0], ret[1] * 1, 1);
    return v1;
  }
  /**
   * @param {Uint8Array} data
   * @param {PacketKind} kind
   */
  constructor(data, kind) {
    const ptr0 = passArray8ToWasm0(data, wasm.__wbindgen_malloc);
    const len0 = WASM_VECTOR_LEN;
    const ret = wasm.packet_new(ptr0, len0, kind);
    this.__wbg_ptr = ret;
    PacketFinalization.register(this, this.__wbg_ptr, this);
    return this;
  }
};
if (Symbol.dispose) Packet.prototype[Symbol.dispose] = Packet.prototype.free;
var PacketKind = Object.freeze({
  Reliable: 0,
  "0": "Reliable",
  Unreliable: 1,
  "1": "Unreliable",
  Unsequenced: 2,
  "2": "Unsequenced",
  ReliableUnsequenced: 3,
  "3": "ReliableUnsequenced",
  UnreliableFragment: 4,
  "4": "UnreliableFragment"
});
var PeerState = Object.freeze({
  Disconnected: 0,
  "0": "Disconnected",
  Connecting: 1,
  "1": "Connecting",
  AcknowledgingConnect: 2,
  "2": "AcknowledgingConnect",
  ConnectionPending: 3,
  "3": "ConnectionPending",
  ConnectionSucceeded: 4,
  "4": "ConnectionSucceeded",
  Connected: 5,
  "5": "Connected",
  DisconnectLater: 6,
  "6": "DisconnectLater",
  Disconnecting: 7,
  "7": "Disconnecting",
  AcknowledgingDisconnect: 8,
  "8": "AcknowledgingDisconnect",
  Zombie: 9,
  "9": "Zombie"
});
function crc32(buffers) {
  const ret = wasm.crc32(buffers);
  return ret >>> 0;
}
function mtuMax() {
  const ret = wasm.mtuMax();
  return ret >>> 0;
}
function push_incoming_packet(host_id, ip, port, data) {
  const ptr0 = passStringToWasm0(ip, wasm.__wbindgen_malloc, wasm.__wbindgen_realloc);
  const len0 = WASM_VECTOR_LEN;
  const ptr1 = passArray8ToWasm0(data, wasm.__wbindgen_malloc);
  const len1 = WASM_VECTOR_LEN;
  wasm.push_incoming_packet(host_id, ptr0, len0, port, ptr1, len1);
}
function timeSinceEpoch() {
  const ret = wasm.timeSinceEpoch();
  return ret >>> 0;
}
function __wbg_get_imports() {
  const import0 = {
    __proto__: null,
    __wbg___wbindgen_throw_344f42d3211c4765: function(arg0, arg1) {
      throw new Error(getStringFromWasm0(arg0, arg1));
    },
    __wbg_call_44b7209e1e252e6a: function() {
      return handleError(function(arg0, arg1, arg2, arg3, arg4) {
        const ret = arg0.call(arg1, arg2, arg3, arg4);
        return ret;
      }, arguments);
    },
    __wbg_error_e5023c0885686c48: function(arg0, arg1) {
      console.error(getStringFromWasm0(arg0, arg1));
    },
    __wbg_get_507a50627bffa49b: function(arg0, arg1) {
      const ret = arg0[arg1 >>> 0];
      return ret;
    },
    __wbg_instanceof_Uint8Array_309b927aaf7a3fc7: function(arg0) {
      let result;
      try {
        result = arg0 instanceof Uint8Array;
      } catch (_) {
        result = false;
      }
      const ret = result;
      return ret;
    },
    __wbg_length_1f0964f4a5e2c6d8: function(arg0) {
      const ret = arg0.length;
      return ret;
    },
    __wbg_length_370319915dc99107: function(arg0) {
      const ret = arg0.length;
      return ret;
    },
    __wbg_new_from_slice_77cdfb7977362f3c: function(arg0, arg1) {
      const ret = new Uint8Array(getArrayU8FromWasm0(arg0, arg1));
      return ret;
    },
    __wbg_now_86c0d4ba3fa605b8: function() {
      const ret = Date.now();
      return ret;
    },
    __wbg_prototypesetcall_4770620bbe4688a0: function(arg0, arg1, arg2) {
      Uint8Array.prototype.set.call(getArrayU8FromWasm0(arg0, arg1), arg2);
    },
    __wbindgen_cast_0000000000000001: function(arg0) {
      const ret = arg0;
      return ret;
    },
    __wbindgen_cast_0000000000000002: function(arg0, arg1) {
      const ret = getStringFromWasm0(arg0, arg1);
      return ret;
    },
    __wbindgen_init_externref_table: function() {
      const table = wasm.__wbindgen_externrefs;
      const offset = table.grow(4);
      table.set(0, void 0);
      table.set(offset + 0, void 0);
      table.set(offset + 1, null);
      table.set(offset + 2, true);
      table.set(offset + 3, false);
    }
  };
  return {
    __proto__: null,
    "./growtopia_wasm_bg.js": import0
  };
}
var EventFinalization = typeof FinalizationRegistry === "undefined" ? { register: () => {
}, unregister: () => {
} } : new FinalizationRegistry((ptr) => wasm.__wbg_event_free(ptr, 1));
var HostFinalization = typeof FinalizationRegistry === "undefined" ? { register: () => {
}, unregister: () => {
} } : new FinalizationRegistry((ptr) => wasm.__wbg_host_free(ptr, 1));
var JsHostSettingsFinalization = typeof FinalizationRegistry === "undefined" ? { register: () => {
}, unregister: () => {
} } : new FinalizationRegistry((ptr) => wasm.__wbg_jshostsettings_free(ptr, 1));
var PacketFinalization = typeof FinalizationRegistry === "undefined" ? { register: () => {
}, unregister: () => {
} } : new FinalizationRegistry((ptr) => wasm.__wbg_packet_free(ptr, 1));
function addToExternrefTable0(obj) {
  const idx = wasm.__externref_table_alloc();
  wasm.__wbindgen_externrefs.set(idx, obj);
  return idx;
}
function _assertClass(instance, klass) {
  if (!(instance instanceof klass)) {
    throw new Error(`expected instance of ${klass.name}`);
  }
}
function getArrayU8FromWasm0(ptr, len) {
  ptr = ptr >>> 0;
  return getUint8ArrayMemory0().subarray(ptr / 1, ptr / 1 + len);
}
function getStringFromWasm0(ptr, len) {
  return decodeText(ptr >>> 0, len);
}
var cachedUint8ArrayMemory0 = null;
function getUint8ArrayMemory0() {
  if (cachedUint8ArrayMemory0 === null || cachedUint8ArrayMemory0.byteLength === 0) {
    cachedUint8ArrayMemory0 = new Uint8Array(wasm.memory.buffer);
  }
  return cachedUint8ArrayMemory0;
}
function handleError(f, args) {
  try {
    return f.apply(this, args);
  } catch (e) {
    const idx = addToExternrefTable0(e);
    wasm.__wbindgen_exn_store(idx);
  }
}
function passArray8ToWasm0(arg, malloc) {
  const ptr = malloc(arg.length * 1, 1) >>> 0;
  getUint8ArrayMemory0().set(arg, ptr / 1);
  WASM_VECTOR_LEN = arg.length;
  return ptr;
}
function passStringToWasm0(arg, malloc, realloc) {
  if (realloc === void 0) {
    const buf = cachedTextEncoder.encode(arg);
    const ptr2 = malloc(buf.length, 1) >>> 0;
    getUint8ArrayMemory0().subarray(ptr2, ptr2 + buf.length).set(buf);
    WASM_VECTOR_LEN = buf.length;
    return ptr2;
  }
  let len = arg.length;
  let ptr = malloc(len, 1) >>> 0;
  const mem = getUint8ArrayMemory0();
  let offset = 0;
  for (; offset < len; offset++) {
    const code = arg.charCodeAt(offset);
    if (code > 127) break;
    mem[ptr + offset] = code;
  }
  if (offset !== len) {
    if (offset !== 0) {
      arg = arg.slice(offset);
    }
    ptr = realloc(ptr, len, len = offset + arg.length * 3, 1) >>> 0;
    const view = getUint8ArrayMemory0().subarray(ptr + offset, ptr + len);
    const ret = cachedTextEncoder.encodeInto(arg, view);
    offset += ret.written;
    ptr = realloc(ptr, len, offset, 1) >>> 0;
  }
  WASM_VECTOR_LEN = offset;
  return ptr;
}
function takeFromExternrefTable0(idx) {
  const value = wasm.__wbindgen_externrefs.get(idx);
  wasm.__externref_table_dealloc(idx);
  return value;
}
var cachedTextDecoder = new TextDecoder("utf-8", { ignoreBOM: true, fatal: true });
cachedTextDecoder.decode();
var MAX_SAFARI_DECODE_BYTES = 2146435072;
var numBytesDecoded = 0;
function decodeText(ptr, len) {
  numBytesDecoded += len;
  if (numBytesDecoded >= MAX_SAFARI_DECODE_BYTES) {
    cachedTextDecoder = new TextDecoder("utf-8", { ignoreBOM: true, fatal: true });
    cachedTextDecoder.decode();
    numBytesDecoded = len;
  }
  return cachedTextDecoder.decode(getUint8ArrayMemory0().subarray(ptr, ptr + len));
}
var cachedTextEncoder = new TextEncoder();
if (!("encodeInto" in cachedTextEncoder)) {
  cachedTextEncoder.encodeInto = function(arg, view) {
    const buf = cachedTextEncoder.encode(arg);
    view.set(buf);
    return {
      read: arg.length,
      written: buf.length
    };
  };
}
var WASM_VECTOR_LEN = 0;
var wasmModule;
var wasmInstance;
var wasm;
function __wbg_finalize_init(instance, module) {
  wasmInstance = instance;
  wasm = instance.exports;
  wasmModule = module;
  cachedUint8ArrayMemory0 = null;
  wasm.__wbindgen_start();
  return wasm;
}
async function __wbg_load(module, imports) {
  if (typeof Response === "function" && module instanceof Response) {
    if (typeof WebAssembly.instantiateStreaming === "function") {
      try {
        return await WebAssembly.instantiateStreaming(module, imports);
      } catch (e) {
        const validResponse = module.ok && expectedResponseType(module.type);
        if (validResponse && module.headers.get("Content-Type") !== "application/wasm") {
          console.warn("`WebAssembly.instantiateStreaming` failed because your server does not serve Wasm with `application/wasm` MIME type. Falling back to `WebAssembly.instantiate` which is slower. Original error:\n", e);
        } else {
          throw e;
        }
      }
    }
    const bytes = await module.arrayBuffer();
    return await WebAssembly.instantiate(bytes, imports);
  } else {
    const instance = await WebAssembly.instantiate(module, imports);
    if (instance instanceof WebAssembly.Instance) {
      return { instance, module };
    } else {
      return instance;
    }
  }
  function expectedResponseType(type) {
    switch (type) {
      case "basic":
      case "cors":
      case "default":
        return true;
    }
    return false;
  }
}
async function __wbg_init(module_or_path) {
  if (wasm !== void 0) return wasm;
  if (module_or_path !== void 0) {
    if (Object.getPrototypeOf(module_or_path) === Object.prototype) {
      ({ module_or_path } = module_or_path);
    } else {
      console.warn("using deprecated parameters for the initialization function; pass a single object instead");
    }
  }
  if (module_or_path === void 0) {
    module_or_path = new URL("growtopia_wasm_bg.wasm", import.meta.url);
  }
  const imports = __wbg_get_imports();
  if (typeof module_or_path === "string" || typeof Request === "function" && module_or_path instanceof Request || typeof URL === "function" && module_or_path instanceof URL) {
    module_or_path = fetch(module_or_path);
  }
  const { instance, module } = await __wbg_load(await module_or_path, imports);
  return __wbg_finalize_init(instance, module);
}

// lib/src/Peer.ts
var Peer = class {
  host;
  /** The unique numeric ID assigned to this peer by the ENet host. */
  id;
  /**
   * Instantiates a new Peer wrapper.
   *
   * @param host - Underlying WebAssembly Host instance.
   * @param id - Numeric peer ID.
   */
  constructor(host, id) {
    this.host = host;
    this.id = id;
  }
  /**
   * Sends a WebAssembly ENet packet to the peer.
   *
   * @param packet - ENet Packet instance to transmit.
   * @param channel - ENet channel ID (defaults to 0).
   */
  send(packet, channel = 0) {
    this.host.peerSend(this.id, channel, packet);
  }
  /**
   * Encapsulates raw byte data into an ENet Packet and transmits it to the peer.
   *
   * @param data - Byte array containing payload data.
   * @param channel - ENet channel ID (defaults to 0).
   * @param flags - Reliability flag (0 = Unreliable, 1 = Reliable, 2 = Unsequenced).
   */
  sendRaw(data, channel = 0, flags = 1) {
    let kind = PacketKind.Reliable;
    if (flags === 0) kind = PacketKind.Unreliable;
    else if (flags === 2) kind = PacketKind.Unsequenced;
    const packet = new Packet(data, kind);
    this.host.peerSend(this.id, channel, packet);
  }
  /**
   * Initiates a graceful disconnect request for this peer.
   *
   * @param data - Optional integer disconnect code.
   */
  disconnect(data = 0) {
    this.host.peerDisconnect(this.id, data);
  }
  /**
   * Forces an immediate abrupt disconnect for this peer without waiting for acknowledgments.
   *
   * @param data - Optional integer disconnect code.
   */
  disconnectNow(data = 0) {
    this.host.peerDisconnectNow(this.id, data);
  }
  /**
   * Schedules a disconnect for this peer after all queued outgoing packets are delivered.
   *
   * @param data - Optional integer disconnect code.
   */
  disconnectLater(data = 0) {
    this.host.peerDisconnectLater(this.id, data);
  }
  /**
   * Sends a ping request to update peer round-trip time calculations.
   */
  ping() {
    this.host.peerPing(this.id);
  }
  /**
   * Resets peer connection state immediately, clearing allocated core resources.
   */
  reset() {
    this.host.peerReset(this.id);
  }
  /**
   * Gets the current ENet connection state for this peer.
   */
  get state() {
    return this.host.peerState(this.id);
  }
  /**
   * Gets the remote IP address of the peer.
   */
  get ip() {
    return this.host.peerAddressIp(this.id);
  }
  /**
   * Gets the remote UDP port number of the peer.
   */
  get port() {
    return this.host.peerAddressPort(this.id);
  }
  /**
   * Gets the current round-trip time (RTT) in milliseconds.
   */
  get rtt() {
    return this.host.peerRoundTripTimeMs(this.id);
  }
};

// lib/src/node-transport.ts
import { createSocket } from "dgram";

// node_modules/nanoevents/index.js
var createNanoEvents = () => ({
  emit(event, ...args) {
    for (let callbacks = this.events[event] || [], i = 0, length = callbacks.length; i < length; i++) {
      callbacks[i](...args);
    }
  },
  events: {},
  on(event, cb) {
    ;
    (this.events[event] ||= []).push(cb);
    return () => {
      this.events[event] = this.events[event]?.filter((i) => cb !== i);
    };
  }
});

// lib/utils/Collection.ts
var Collection = class extends Map {
  /** If this collection is empty. */
  get empty() {
    return this.size === 0;
  }
  every(predicate, thisArg) {
    return this.toArray().every(predicate, thisArg);
  }
  filter(predicate, thisArg) {
    return this.toArray().filter(predicate, thisArg);
  }
  find(predicate, thisArg) {
    return this.toArray().find(predicate, thisArg);
  }
  /** See: {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/findIndex | Array#findIndex } */
  findIndex(predicate, thisArg) {
    return this.toArray().findIndex(predicate, thisArg);
  }
  first(amount) {
    if (amount === void 0) {
      const iterable2 = this.values();
      return iterable2.next().value;
    }
    if (amount < 0) {
      return this.last(amount * -1);
    }
    amount = Math.min(amount, this.size);
    const iterable = this.values();
    return Array.from({ length: amount }, () => iterable.next().value);
  }
  last(amount) {
    const iterator = Array.from(this.values());
    if (amount === void 0) {
      return iterator.at(-1);
    }
    if (amount < 0) {
      return this.first(amount * -1);
    }
    if (!amount) {
      return [];
    }
    return iterator.slice(-amount);
  }
  /** See: {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/map | Array#map } */
  map(predicate, thisArg) {
    return this.toArray().map(predicate, thisArg);
  }
  /**
   * Pick a random element from the collection, or undefined if the collection is empty.
   */
  random() {
    if (this.empty) {
      return void 0;
    }
    const iterable = Array.from(this.values());
    return iterable[Math.floor(Math.random() * iterable.length)];
  }
  reduce(predicate, initialValue) {
    return this.toArray().reduce(predicate, initialValue);
  }
  reduceRight(predicate, initialValue) {
    return this.toArray().reduceRight(predicate, initialValue);
  }
  /** See: {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/some | Array#some } */
  some(predicate, thisArg) {
    return this.toArray().some(predicate, thisArg);
  }
  /** Get the values of this collection as an array. */
  toArray() {
    return Array.from(this.values());
  }
};

// lib/src/node-transport.ts
var NodeENetHost = class {
  socket;
  bindIp;
  bindPort;
  timer;
  /** Event emitter instance for subscribing to connect, disconnect, and receive events. */
  emitter;
  /** Underlying WebAssembly Host instance. */
  host;
  /** In-memory cache holding active Peer wrapper instances. */
  cache = {
    peers: new Collection()
  };
  constructor(bindIp, bindPort, settings) {
    this.bindIp = bindIp;
    this.bindPort = bindPort;
    this.socket = createSocket("udp4");
    this.emitter = createNanoEvents();
    const sendCallback = (ip, port, data) => {
      const copiedData = Uint8Array.from(data);
      this.socket.send(copiedData, port, ip);
    };
    this.host = new Host(bindIp, bindPort, settings, sendCallback);
    this.socket.on("message", (msg, rinfo) => {
      push_incoming_packet(this.host.id, rinfo.address, rinfo.port, new Uint8Array(msg));
    });
    this.socket.on("error", (err) => {
      throw err;
    });
  }
  getPeer(id) {
    if (!this.cache.peers.has(id)) {
      this.cache.peers.set(id, new Peer(this.host, id));
    }
    return this.cache.peers.get(id);
  }
  /**
   * Binds the underlying UDP socket to the configured IP address and port.
   *
   * @returns A promise resolving when the socket binding is established.
   */
  start() {
    return new Promise((resolve, reject) => {
      const onError = (err) => {
        reject(err);
      };
      this.socket.once("error", onError);
      if (this.bindPort > 0) {
        this.socket.bind(this.bindPort, this.bindIp, () => {
          this.socket.removeListener("error", onError);
          resolve();
        });
      } else {
        this.socket.bind(() => {
          this.socket.removeListener("error", onError);
          resolve();
        });
      }
    });
  }
  /**
   * Dispatches pending ENet network events synchronously.
   */
  service() {
    return this.host.service();
  }
  /**
   * Checks for queued ENet events.
   */
  checkEvents() {
    return this.host.checkEvents();
  }
  /**
   * Starts an asynchronous polling loop to process incoming and outgoing ENet events at regular intervals.
   *
   * @param ms - Polling interval duration in milliseconds (default: 15ms).
   * @param options - Polling options such as auto-freeing received packets.
   */
  startPolling(ms = 15, options = {}) {
    const autoFreePacket = options.autoFreePacket !== false;
    if (this.timer) return;
    this.timer = setInterval(() => {
      let event;
      while ((event = this.host.service()) && event.eventType !== 0) {
        switch (event.eventType) {
          case 1: {
            const peer = this.getPeer(event.peerId);
            this.emitter.emit("connect", peer);
            break;
          }
          case 2: {
            const peer = this.getPeer(event.peerId);
            const data = event.data;
            this.emitter.emit("disconnect", peer, data);
            this.cache.peers.delete(event.peerId);
            break;
          }
          case 3: {
            const peer = this.getPeer(event.peerId);
            const packet = event.packet();
            const channelId = event.channelId;
            this.emitter.emit("receive", peer, packet, channelId);
            if (autoFreePacket) packet.free();
            break;
          }
        }
      }
    }, ms);
  }
  /**
   * Stops the active event polling loop.
   */
  stopPolling() {
    if (this.timer) {
      clearInterval(this.timer);
      this.timer = void 0;
    }
  }
  /**
   * Connects to a remote ENet host endpoint.
   *
   * @param ip - Target host IP address.
   * @param port - Target host UDP port.
   * @param channelCount - Number of ENet channels to allocate.
   * @param data - User data integer passed in connect request.
   * @returns Connected Peer wrapper instance.
   */
  connect(ip, port, channelCount, data) {
    const peerId = this.host.connect(ip, port, channelCount, data);
    return this.getPeer(peerId);
  }
};
var NodeENetClient = class extends NodeENetHost {
  constructor(bindIp = "0.0.0.0", bindPort = 0, peerCount = 1, channelLimit = 2) {
    const settings = new JsHostSettings();
    settings.peerLimit = peerCount;
    settings.channelLimit = channelLimit;
    settings.usingNewPacket = true;
    settings.usingNewPacketServer = false;
    super(bindIp, bindPort, settings);
  }
};
var NodeENetServer = class extends NodeENetHost {
  constructor(bindIp, bindPort, peerCount, channelLimit) {
    const settings = new JsHostSettings();
    settings.peerLimit = peerCount;
    settings.channelLimit = channelLimit;
    settings.usingNewPacket = false;
    settings.usingNewPacketServer = true;
    super(bindIp, bindPort, settings);
  }
};

// lib/src/browser-transport.ts
var BrowserENetHost = class {
  dataChannel;
  timer;
  /** Event emitter instance for subscribing to connect, disconnect, and receive events. */
  emitter;
  /** Underlying WebAssembly Host instance. */
  host;
  /** In-memory cache holding active Peer wrapper instances. */
  cache = {
    peers: new Collection()
  };
  constructor(settings) {
    this.emitter = createNanoEvents();
    const sendCallback = (ip, port, data) => {
      if (!this.dataChannel || this.dataChannel.readyState !== "open") {
        throw new Error("RTCDataChannel is not open or attached.");
      }
      this.dataChannel.send(data);
    };
    this.host = new Host("0.0.0.0", 0, settings, sendCallback);
  }
  getPeer(id) {
    if (!this.cache.peers.has(id)) {
      this.cache.peers.set(id, new Peer(this.host, id));
    }
    return this.cache.peers.get(id);
  }
  /**
   * Attaches a WebRTC RTCDataChannel for transporting ENet packet payloads.
   *
   * @param channel - Open RTCDataChannel instance.
   */
  attachDataChannel(channel) {
    this.dataChannel = channel;
    this.dataChannel.binaryType = "arraybuffer";
    this.dataChannel.addEventListener("message", (event) => {
      push_incoming_packet(this.host.id, "192.168.1.1", 1234, new Uint8Array(event.data));
    });
    this.dataChannel.addEventListener("error", (event) => {
      throw event;
    });
  }
  /**
   * Initiates a connection request to a peer endpoint over WebRTC.
   *
   * @param ip - Target host IP address.
   * @param port - Target host port.
   * @param channelCount - Number of ENet channels to allocate.
   * @param data - User data integer passed in connect request.
   * @returns Connected Peer wrapper instance.
   */
  connect(ip, port, channelCount, data) {
    const peerId = this.host.connect(ip, port, channelCount, data);
    return this.getPeer(peerId);
  }
  /**
   * Dispatches pending ENet network events synchronously.
   */
  service() {
    return this.host.service();
  }
  /**
   * Checks for queued ENet events.
   */
  checkEvents() {
    return this.host.checkEvents();
  }
  /**
   * Starts an asynchronous polling loop to process ENet events.
   *
   * @param ms - Polling interval duration in milliseconds (default: 15ms).
   * @param options - Polling options such as auto-freeing received packets.
   */
  startPolling(ms = 15, options = {}) {
    const autoFreePacket = options.autoFreePacket !== false;
    if (this.timer) return;
    this.timer = setInterval(() => {
      let event;
      while ((event = this.host.service()) && event.eventType !== 0) {
        switch (event.eventType) {
          case 1: {
            const peer = this.getPeer(event.peerId);
            this.emitter.emit("connect", peer);
            break;
          }
          case 2: {
            const peer = this.getPeer(event.peerId);
            const data = event.data;
            this.emitter.emit("disconnect", peer, data);
            this.cache.peers.delete(event.peerId);
            break;
          }
          case 3: {
            const peer = this.getPeer(event.peerId);
            const packet = event.packet();
            const channelId = event.channelId;
            this.emitter.emit("receive", peer, packet, channelId);
            if (autoFreePacket) packet.free();
            break;
          }
        }
      }
    }, ms);
  }
  /**
   * Stops the active event polling loop.
   */
  stopPolling() {
    if (this.timer) {
      clearInterval(this.timer);
      this.timer = void 0;
    }
  }
};
var BrowserENetClient = class extends BrowserENetHost {
  constructor(peerCount = 1, channelLimit = 2) {
    const settings = new JsHostSettings();
    settings.peerLimit = peerCount;
    settings.channelLimit = channelLimit;
    settings.usingNewPacket = true;
    settings.usingNewPacketServer = false;
    super(settings);
  }
};
var BrowserENetServer = class extends BrowserENetHost {
  constructor(peerCount, channelLimit) {
    const settings = new JsHostSettings();
    settings.peerLimit = peerCount;
    settings.channelLimit = channelLimit;
    settings.usingNewPacket = false;
    settings.usingNewPacketServer = true;
    super(settings);
  }
};

// lib/utils/ExtendBuffer.ts
var ExtendBuffer = class {
  constructor(data, mempos = 0) {
    this.mempos = mempos;
    this.data = Array.isArray(data) ? data : Array.from({ length: data }, () => 0);
  }
  mempos;
  data;
  read(size) {
    let value = 0;
    for (let i = 0; i < size; i++) {
      value |= this.data[this.mempos + i] << i * 8;
    }
    this.mempos += size;
    return value >>> 0;
  }
  readSigned(size) {
    return this.read(size) << 0;
  }
  write(value, size) {
    for (let i = 0; i < size; i++) {
      this.data[this.mempos + i] = value >> i * 8 & 255;
    }
    this.mempos += size;
  }
  readU8 = () => this.read(1);
  readU16 = (be = false) => be ? this.readBE(2) : this.read(2);
  readU32 = (be = false) => be ? this.readBE(4) : this.read(4);
  readI8 = () => this.readSigned(1);
  readI16 = (be = false) => be ? this.readSignedBE(2) : this.readSigned(2);
  readI32 = (be = false) => be ? this.readSignedBE(4) : this.readSigned(4);
  readBE(size) {
    let value = 0;
    for (let i = 0; i < size; i++) {
      value = value << 8 | this.data[this.mempos + i];
    }
    this.mempos += size;
    return value >>> 0;
  }
  readSignedBE(size) {
    return this.readBE(size) << 0;
  }
  writeU8 = (value) => this.write(value, 1);
  writeU16 = (value, be = false) => be ? this.writeBE(value, 2) : this.write(value, 2);
  writeU32 = (value, be = false) => be ? this.writeBE(value, 4) : this.write(value, 4);
  writeI8 = (value) => this.write(value, 1);
  writeI16 = (value, be = false) => be ? this.writeBE(value, 2) : this.write(value, 2);
  writeI32 = (value, be = false) => be ? this.writeBE(value, 4) : this.write(value, 4);
  writeBE(value, size) {
    for (let i = 0; i < size; i++) {
      this.data[this.mempos + i] = value >> (size - 1 - i) * 8 & 255;
    }
    this.mempos += size;
  }
  writeU = (size, value, be = false) => {
    const methods = { 1: this.writeU8, 2: this.writeU16, 4: this.writeU32 };
    methods[size](value, be);
  };
  writeI = (size, value, be = false) => {
    const methods = { 1: this.writeI8, 2: this.writeI16, 4: this.writeI32 };
    methods[size](value, be);
  };
  async readString(be = false) {
    const len = be ? this.readBE(2) : this.read(2);
    const chars = this.data.slice(this.mempos, this.mempos + len);
    this.mempos += len;
    return String.fromCharCode(...chars);
  }
  async writeString(str, be = false) {
    const bytes = str.split("").map((char) => char.charCodeAt(0));
    if (be) {
      this.writeBE(str.length, 2);
    } else {
      this.write(str.length, 2);
    }
    for (const byte of bytes) {
      this.data[this.mempos++] = byte;
    }
  }
};

// lib/src/packets/TankPacket.ts
var TANK_HEADER_SIZE = 60;
var TankPacket = class _TankPacket {
  /**
   * Creates a new instance of the TankPacket class.
   * @param data The tankpacket header and data.
   */
  constructor(data) {
    this.data = data;
  }
  data;
  /**
   * Creates a new instance of the TankPacket class.
   * @param data TankPacket header and data.
   */
  static from(data) {
    return new _TankPacket(data);
  }
  /**
   * Converts a buffer to a tank packet.
   * @param buf The buffer to convert.
   */
  static fromBuffer(buf) {
    const data = {
      packetType: buf.readUint32LE(0),
      type: buf.readUInt8(4),
      punchID: buf.readUInt8(5),
      buildRange: buf.readUInt8(6),
      punchRange: buf.readUInt8(7),
      netID: buf.readInt32LE(8),
      targetNetID: buf.readInt32LE(12),
      state: buf.readUInt32LE(16),
      info: buf.readInt32LE(24),
      xPos: buf.readFloatLE(28),
      yPos: buf.readFloatLE(32),
      xSpeed: buf.readFloatLE(36),
      ySpeed: buf.readFloatLE(40),
      xPunch: buf.readInt32LE(48),
      yPunch: buf.readInt32LE(52)
    };
    const dataLength = buf.readUInt32LE(56);
    if (dataLength > 0) data.data = () => buf.subarray(60, 60 + dataLength);
    return new _TankPacket(data);
  }
  /**
   * Converts the TankPacket class to a Buffer
   */
  parse() {
    if (!this.data) return;
    let buf = Buffer.alloc(TANK_HEADER_SIZE);
    buf.writeUInt32LE(4);
    buf.writeUint8(this.data.type ?? 0, 4);
    buf.writeUint8(this.data.punchID ?? 0, 5);
    buf.writeUint8(this.data.buildRange ?? 0, 6);
    buf.writeUint8(this.data.punchRange ?? 0, 7);
    buf.writeInt32LE(this.data.netID ?? 0, 8);
    buf.writeInt32LE(this.data.targetNetID ?? 0, 12);
    buf.writeUInt32LE(this.data.state ?? 8, 16);
    buf.writeInt32LE(this.data.info ?? 0, 24);
    buf.writeFloatLE(this.data.xPos ?? 0, 28);
    buf.writeFloatLE(this.data.yPos ?? 0, 32);
    buf.writeFloatLE(this.data.xSpeed ?? 0, 36);
    buf.writeFloatLE(this.data.ySpeed ?? 0, 40);
    buf.writeInt32LE(this.data.xPunch ?? 0, 48);
    buf.writeInt32LE(this.data.yPunch ?? 0, 52);
    if (typeof this.data.data === "function") {
      const extra = this.data.data();
      if (!Buffer.isBuffer(extra)) return;
      buf.writeUInt32LE(extra.length, 56);
      buf = Buffer.concat([buf, extra]);
    }
    return buf;
  }
};

// lib/src/packets/TextPacket.ts
var TextPacket = class _TextPacket {
  /**
   * Creates a new TextPacket class
   * @param type The type of the packet.
   * @param strings An array of strings to include, they will be joined by a newline character. (`\n`)
   */
  constructor(type, strings) {
    this.type = type;
    this.strings = strings;
  }
  type;
  strings;
  /**
   * Creates a TextPacket class.
   * @param type The type of the packet.
   * @param strings Strings to include to the packet. They are not arrays, but instead they are arguments for the function.
   */
  static from(type, ...strings) {
    return new _TextPacket(type, strings);
  }
  /**
   * Creates a TextPacket class from a Buffer.
   * @param packet The buffer to convert.
   */
  static fromBuffer(packet) {
    if (packet.length < 4) throw new Error("Invalid packet received.");
    const type = packet.readUInt32LE();
    const str = packet.toString("utf-8", 4).slice(0, -1) ?? "";
    return new _TextPacket(type, str.split("\n"));
  }
  /**
   * Parses the TextPacket and converts it to a buffer.
   */
  parse() {
    const str = this.strings.join("\n");
    const buffer = Buffer.alloc(4 + str.length + 1);
    buffer.writeUInt32LE(this.type);
    buffer.write(str, 4);
    return buffer;
  }
};

// lib/src/Constants.ts
var VariantTypes = /* @__PURE__ */ ((VariantTypes2) => {
  VariantTypes2[VariantTypes2["NONE"] = 0] = "NONE";
  VariantTypes2[VariantTypes2["FLOAT_1"] = 1] = "FLOAT_1";
  VariantTypes2[VariantTypes2["STRING"] = 2] = "STRING";
  VariantTypes2[VariantTypes2["FLOAT_2"] = 3] = "FLOAT_2";
  VariantTypes2[VariantTypes2["FLOAT_3"] = 4] = "FLOAT_3";
  VariantTypes2[VariantTypes2["UNSIGNED_INT"] = 5] = "UNSIGNED_INT";
  VariantTypes2[VariantTypes2["SIGNED_INT"] = 9] = "SIGNED_INT";
  return VariantTypes2;
})(VariantTypes || {});
var PacketTypes = /* @__PURE__ */ ((PacketTypes2) => {
  PacketTypes2[PacketTypes2["UNK"] = 0] = "UNK";
  PacketTypes2[PacketTypes2["HELLO"] = 1] = "HELLO";
  PacketTypes2[PacketTypes2["STR"] = 2] = "STR";
  PacketTypes2[PacketTypes2["ACTION"] = 3] = "ACTION";
  PacketTypes2[PacketTypes2["TANK"] = 4] = "TANK";
  PacketTypes2[PacketTypes2["ERROR"] = 5] = "ERROR";
  PacketTypes2[PacketTypes2["TRACK"] = 6] = "TRACK";
  PacketTypes2[PacketTypes2["CLIENT_LOG_REQ"] = 7] = "CLIENT_LOG_REQ";
  PacketTypes2[PacketTypes2["CLIENT_LOG_RES"] = 8] = "CLIENT_LOG_RES";
  return PacketTypes2;
})(PacketTypes || {});

// lib/src/packets/Variant.ts
var Variant = class _Variant {
  /**
   * Creates a new instance of the Variant class.
   * @param options The options for the variant.
   * @param args The arguments of the Variant.
   */
  constructor(options = {}, args) {
    this.options = options;
    this.args = args;
  }
  options;
  args;
  index = 0;
  /**
   * Creates a new Variant class.
   * @param opts The options for the variant.
   * @param args The arguments of the Variant.
   */
  static from(opts, ...args) {
    if (typeof opts === "string" || typeof opts === "number" || Array.isArray(opts)) {
      args.unshift(opts);
      opts = { netID: -1, delay: 0 };
    }
    return new _Variant(opts, args);
  }
  static toArray(data) {
    let arr = [];
    let pos = 60;
    const count = data.readUint8(60);
    pos += 1;
    for (let i = 1; i <= count; i++) {
      const index = data.readUint8(pos);
      pos += 1;
      const type = data.readUint8(pos);
      const typeName = VariantTypes[type];
      pos += 1;
      switch (type) {
        case 2 /* STRING */: {
          const strLength = data.readUint32LE(pos);
          pos += 4;
          const value = data.subarray(pos, pos + strLength).toString();
          pos += strLength;
          arr.push({ index, type, typeName, value });
          break;
        }
        case 5 /* UNSIGNED_INT */: {
          const value = data.readUint32LE(pos);
          pos += 4;
          arr.push({ index, type, typeName, value });
          break;
        }
        case 9 /* SIGNED_INT */: {
          const value = data.readInt32LE(pos);
          pos += 4;
          arr.push({ index, type, typeName, value });
          break;
        }
        case 1 /* FLOAT_1 */: {
          const value = [data.readFloatLE(pos)];
          pos += 4;
          arr.push({ index, type, typeName, value });
          break;
        }
        case 3 /* FLOAT_2 */: {
          let value = [];
          for (let j = 1; j <= 2; j++) {
            value.push(data.readFloatLE(pos));
            pos += 4;
          }
          arr.push({ index, type, typeName, value });
          break;
        }
        case 4 /* FLOAT_3 */: {
          let value = [];
          for (let j = 1; j <= 3; j++) {
            value.push(data.readFloatLE(pos));
            pos += 4;
          }
          arr.push({ index, type, typeName, value });
          break;
        }
      }
    }
    return arr;
  }
  /**
   * Parses the data of the Variant and returns a TankPacket from it.
   */
  parse() {
    let buf = [this.args.length];
    this.args.forEach((arg) => {
      buf.push(this.index++);
      switch (typeof arg) {
        case "string": {
          buf.push(2 /* STRING */);
          const bytes = new Uint32Array(1);
          bytes[0] = arg.length;
          const uint8_buf = new Uint8ClampedArray(bytes.buffer);
          const text_buf = new TextEncoder().encode(arg);
          buf = [...buf, ...Array.from(uint8_buf), ...Array.from(text_buf)];
          break;
        }
        case "number": {
          let bytes;
          if (arg < 0) {
            bytes = new Int32Array(1);
            buf.push(9 /* SIGNED_INT */);
          } else {
            bytes = new Uint32Array(1);
            buf.push(5 /* UNSIGNED_INT */);
          }
          bytes[0] = arg;
          const uint8_buf = new Uint8ClampedArray(bytes.buffer);
          buf = [...buf, ...Array.from(uint8_buf)];
          break;
        }
        case "object": {
          if (!Array.isArray(arg)) return;
          const floatTypeKey = `FLOAT_${arg.length}`;
          const type = VariantTypes[floatTypeKey];
          if (type === void 0) return;
          buf.push(type);
          arg.forEach((float) => {
            const bytes = new Float32Array(1);
            bytes[0] = float;
            const uint8_buf = new Uint8ClampedArray(bytes.buffer);
            buf = [...buf, ...Array.from(uint8_buf)];
          });
          break;
        }
      }
    });
    return TankPacket.from({
      type: 1,
      netID: this.options.netID ?? -1,
      info: this.options.delay ?? 0,
      data: () => Buffer.from(buf)
    });
  }
};

// lib/index.ts
async function init(input) {
  if (!input && typeof process !== "undefined" && process.versions && process.versions.node) {
    try {
      const fs = await import("fs");
      const path2 = await import("path");
      const { fileURLToPath: fileURLToPath2 } = await import("url");
      let wasmPath = "";
      if (typeof __dirname !== "undefined") {
        wasmPath = path2.join(__dirname, "pkg", "growtopia_wasm_bg.wasm");
      } else if (import.meta && import.meta.url) {
        wasmPath = fileURLToPath2(new URL("pkg/growtopia_wasm_bg.wasm", import.meta.url));
      }
      if (wasmPath && fs.existsSync(wasmPath)) {
        input = fs.readFileSync(wasmPath);
      }
    } catch {
    }
  }
  await __wbg_init(input);
}
export {
  BrowserENetClient,
  BrowserENetHost,
  BrowserENetServer,
  Collection,
  ExtendBuffer,
  Host,
  NodeENetClient,
  NodeENetHost,
  NodeENetServer,
  Packet,
  PacketKind,
  PacketTypes,
  Peer,
  PeerState,
  TankPacket,
  TextPacket,
  Variant,
  VariantTypes,
  crc32,
  init,
  mtuMax,
  timeSinceEpoch
};
//# sourceMappingURL=index.mjs.map